There are some docker tools.

* ngxin_return_json 
    - Contains the nginx server, which work as a http server and return json.
    - Contains the python code, which use urllib2 to fetch the nginx server.
